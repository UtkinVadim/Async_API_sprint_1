version: '3.9'
services:
  redis:
    image: redis:6.2.6-alpine
    container_name: redis
    restart: always
    env_file:
      - .env
    ports:
      - "6379:6379"
    networks:
      - movies-network


  #elasticsearch:
  #  image: elasticsearch:7.14.2
  #  container_name: elasticsearch
  #  restart: always
  #  environment:
  #    - "discovery.type=single-node"
  #  env_file:
  #    - .env
  #  ports:
  #    - ${ELASTIC_HOST}:${ELASTIC_PORT}:9200
  #  networks:
  #    - async_api_network

networks:
  movies-network:
    name: movies-network
